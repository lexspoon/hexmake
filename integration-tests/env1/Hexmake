{
  "env": [
    "PATH",
    "TEST_ENV"
  ],
  "rules": [
    {
      "name": "test",
      "inputs": [],
      "outputs": [
        "out/test.txt"
      ],
      "commands": [
        "echo $TEST_ENV > out/test.txt"
      ]
    }
  ]
}